import { GroupArrayByPipe } from './group-by.pipe';

describe('GroupByPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupArrayByPipe();
    expect(pipe).toBeTruthy();
  });
});
